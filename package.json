{
  "name": "quora-question-pairs-mern",
  "version": "1.0.0",
  "private": true,
  "description": "MERN stack duplicate question detector for Quora-style question pairs.",
  "scripts": {
    "dev": "concurrently \"npm run flask:dev\" \"npm run server:dev\" \"npm run client:dev\"",
    "flask:dev": ".venv/bin/python streamlit-app/app.py",
    "server:dev": "npm run dev --prefix server",
    "client:dev": "npm run dev --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
